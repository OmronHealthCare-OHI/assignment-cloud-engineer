# Copy to .env (already gitignored) and fill in.
#
# Free LocalStack account -> https://app.localstack.cloud -> Account -> Auth Tokens.
# Needed to start the localstack container at all (docker-compose.yml), not
# needed for anything else in this repo.
LOCALSTACK_AUTH_TOKEN=
